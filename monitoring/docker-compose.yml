services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - ./targets:/etc/prometheus/targets
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=30d"

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin123
    depends_on:
      - prometheus

  dooray-proxy:
    image: python:3.12-slim
    container_name: dooray-proxy
    restart: unless-stopped
    volumes:
      - ./dooray-proxy/webhook_proxy.py:/app/webhook_proxy.py:ro
    environment:
      - DOORAY_WEBHOOK_URL=https://nhnent.dooray.com/services/3234962574780345705/4266837808044889584/9gifSAtzT4mEo8B0Xjz9Zw
      - LISTEN_PORT=5001
      - PYTHONUNBUFFERED=1
    command: python /app/webhook_proxy.py

volumes:
  prometheus_data:
  grafana_data:
